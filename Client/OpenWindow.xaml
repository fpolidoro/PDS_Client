<UserControl x:Class="Client.OpenWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Client"
             mc:Ignorable="d" 
             d:DesignHeight="30" d:DesignWidth="180">
    <Grid Height="26">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="26"/>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="0.3*" />
        </Grid.ColumnDefinitions>
        <Image x:Name="img_OpenWindowIcon" Grid.Column="0" Width="26" Height="26" HorizontalAlignment="Left"/>
        <TextBlock x:Name="txtb_ProcessName" Grid.Column="1" TextTrimming="WordEllipsis" VerticalAlignment="Center" Margin="4,0,0,0" HorizontalAlignment="Center" />
        <TextBlock x:Name="txtb_ProcessTimeFocused" Grid.Column="2" Margin="4,0,0,0" Width="26" VerticalAlignment="Center" HorizontalAlignment="Right"/>
    </Grid>
</UserControl>
